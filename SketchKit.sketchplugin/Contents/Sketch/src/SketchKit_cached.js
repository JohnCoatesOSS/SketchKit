// This files exists to load SketchKit.js
// When a script file is specified in manifest.json it's cached, so any changes
// made to it after it's been used once will not take effect until Sketch is restared.
// However, when a file is imported like in the line below the cache is bypassed,
// allowing for changes to be made while keeping Sketch open.
@import 'SketchKit.js'
